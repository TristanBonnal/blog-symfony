<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('home') }}">Mon Super Blob</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarColor01" aria-controls="navbarColor01" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarColor01">
        <ul class="navbar-nav me-auto">
            <li class="nav-item">
            <a class="nav-link {{title == 'Accueil' ? 'active' : ''}}" href="{{ path('home') }}">Articles récents</a>
            </li>
            {% if is_granted('ROLE_MODERATOR') %}
                <li class="nav-item">
                    <a class="nav-link {{title == 'Nouvel article' ? 'active' : ''}}" href="{{ path('post_new') }}">Nouveau</a>
                </li>
            {% endif %}
            {% if not app.user %}
            <li class="nav-item">
                <a class="nav-link {{title == 'A propos' ? 'active' : ''}}" href="{{ path('app_login') }}">Connexion</a>
            </li>
            {% else %}
            <li class="nav-item">
                <a class="nav-link {{title == 'A propos' ? 'active' : ''}}" href="{{ path('app_logout') }}">Déconnexion</a>
            </li>
            {% endif %}
        </ul>
        <form method="get" action="{{ path('search_bar') }}" class="d-flex">
            <input name="search" class="form-control" type="text" placeholder="Un article ? Un auteur ?">
            <button class="btn btn-secondary my-2 my-sm-0 mx-2" type="submit">
                <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill="currentColor" class="bi bi-search" viewBox="0 0 16 16">
                <path d="M11.742 10.344a6.5 6.5 0 1 0-1.397 1.398h-.001c.03.04.062.078.098.115l3.85 3.85a1 1 0 0 0 1.415-1.414l-3.85-3.85a1.007 1.007 0 0 0-.115-.1zM12 6.5a5.5 5.5 0 1 1-11 0 5.5 5.5 0 0 1 11 0z"/>
                </svg>
            </button>
        </form>
        </div>
    </div>
</nav>